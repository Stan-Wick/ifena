% Count the number of elements between 50 and 100
CountLast50 = uint8(sum(Random100 >= 50 & Random100 <= 100));

% Generate the count report string
Report = ['There are ' , num2str(CountLast50), ' elements with a value between 50 and 100.'];
CountReport = string(Report);

% Display the count report
disp(CountReport);
